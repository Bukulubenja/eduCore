# Runtime dependencies. Pinned: a reproducible build is a prerequisite for
# being able to say what is running in production.
Django==6.0.7
djangorestframework==3.17.1
drf-spectacular==0.30.0
django-environ==0.14.0
psycopg[binary]==3.3.4
celery==5.6.3
redis==8.1.0
argon2-cffi==25.1.0
PyJWT==2.13.0
PyOTP==2.10.0
reportlab==5.0.0
cryptography==50.0.0

# Serving. gunicorn is the WSGI server in production; whitenoise serves the
# collected static files (operator admin, Swagger UI) without a separate CDN
# at this scale. Neither is imported by application code.
gunicorn==23.0.0
whitenoise==6.11.0
